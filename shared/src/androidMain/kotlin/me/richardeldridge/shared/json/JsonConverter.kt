package me.richardeldridge.shared.json

class JsonConverter {
    fun convertToString(jsonResult: String) {

//        var news:String=gs.toJson(result)
//        File("output.json").writeText(news)
    }
}